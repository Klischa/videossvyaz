<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Сеть -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Foreground-сервис: удержание процесса во время звонка (Android 13+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Медиа -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Контакты (для отправки ссылки выбранному контакту) -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- Аппаратные требования (необязательны, но рекомендуются) -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.P2PCall"
        tools:targetApi="31">

        <!-- Главный экран: точка входа + обработчик deep link. -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.P2PCall">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- App Link (HTTPS) — рекомендуется для мессенджеров. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="yourdomain.com" android:path="/call" />
            </intent-filter>

            <!-- Кастомная схема (удобна для тестирования без домена). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="myapp" android:host="call" />
            </intent-filter>
        </activity>

        <!-- Экран звонка. singleTask: ответная ссылка доходит до уже работающего
             экземпляра (offerer) через onNewIntent, переиспользуя тот же PeerConnection. -->
        <activity
            android:name=".ui.CallActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation|keyboardHidden"
            android:screenOrientation="portrait" />

        <!-- Активити сканера ZXing. exported задаём явно (рекомендуется для
             targetSdk 31+); остальные атрибуты наследуем от библиотеки —
             конфликта при слиянии нет, т.к. библиотека exported не задаёт. -->
        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            android:exported="false" />

        <!-- FileProvider: для отправки QR-кода как файла-изображения. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Foreground-сервис удержания звонка (не даёт системе убить процесс
             во время ожидания ответной ссылки / активного соединения). -->
        <service
            android:name=".ui.CallService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>
</manifest>
