pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // org.webrtc:google-webrtc изначально публиковался в JCenter.
        // JCenter переведён в режим read-only, но старые артефакты всё ещё доступны.
        maven { url 'https://jitpack.io' }
        // ВНИМАНИЕ: если зависимость org.webrtc:google-webrtc не резолвится,
        // раскомментируйте строку ниже (jcenter остаётся читаемым для legacy-артефактов).
        // jcenter()
    }
}

rootProject.name = "P2PCall"
include ':app'
